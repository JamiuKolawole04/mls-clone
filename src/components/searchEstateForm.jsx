import { CustomInput } from "./customInput";

export const SearchEstateForm = ({ heading, subHeading }) => {
  return (
    <div>
      <h3>{heading}</h3>
      <p>{subHeading}</p>
    </div>
  );
};
